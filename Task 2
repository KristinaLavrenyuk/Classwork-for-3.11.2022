
#include <iostream>
using namespace std;

int main()
{
    int n, array[100]{};
    float sum = 0, CA = 0;
    cin >> n;

    for (int i = 0; i < n; i++)
    {
        cin >> array[i];
        sum += array[i];
    }
    CA = (n != 0) ? sum / n : 0;
    cout << CA;
    system("pause");
}
